// x = x + alpha
kernel void addScalarKernel(const iint   n,
          const dfloat alpha,
                dfloat *restrict x){

  for(iint i=0;i<n;++i;tile(256)){
    if(i<n)
      x[i] += alpha;
  }
}
