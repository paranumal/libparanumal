// x = x + alpha
kernel void addScalarKernel(const int   n,
          const dfloat alpha,
                dfloat *restrict x){

  for(int i=0;i<n;++i;tile(256)){
    if(i<n)
      x[i] += alpha;
  }
}
