// x = x + alpha
kernel void addScalarKernel(const dlong   n,
          const dfloat alpha,
                dfloat *restrict x){

  for(dlong i=0;i<n;++i;tile(256)){
    if(i<n)
      x[i] += alpha;
  }
}
