// x = x + alpha
@kernel void addScalarKernel(const dlong   n,
          const dfloat alpha,
                @restrict dfloat * x){

  for(dlong i=0;i<n;++i;@tile(256,@outer,@inner)){
    if(i<n)
      x[i] += alpha;
  }
}
