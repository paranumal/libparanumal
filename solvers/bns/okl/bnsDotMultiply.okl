kernel void bnsDotMultiply(const dlong N,
		            dfloat * restrict v,
		      const dfloat * restrict w ){
  
  for(dlong n=0;n<N;++n;tile(256)){
    if(n<N){
      dfloat q1 = v[n];
      dfloat q2 = w[n];
      v[n]      = q1*q2;
    }
  }

}