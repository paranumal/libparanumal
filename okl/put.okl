
kernel void put(const int N,
		const dfloat * restrict subv,
		const int   * restrict ids,
		dfloat * restrict v){

  for(int n=0;n<N;++n;tile(256)){
    if(n<N){
      const int id = ids[n];
      if (id > -1) {
        v[id] = subv[n];
      }
    }
  }
}
