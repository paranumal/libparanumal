
@kernel void put(const dlong N,
		@restrict const  dfloat *  subv,
		@restrict const  dlong   *  ids,
		@restrict dfloat *  v){

  for(dlong n=0;n<N;++n;@tile(256,@outer,@inner)){
    if(n<N){
      const dlong id = ids[n];
      if (id > -1) {
        v[id] = subv[n];
      }
    }
  }
}
