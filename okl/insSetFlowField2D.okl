
kernel void insSetFlowField2D(const iint Nelements
			      const dfloat t,
			      const dfloat * restrict x,
			      const dfloat * restrict y,
			      const iint offset,
			      const dfloat * restrict U,
			      const dfloat * restrict V,
			      const dfloat * restrict P){


  for(iint n=0;n<p_Np*Nelements;++n;tile(256)){

    if(n<p_Np*Nelements){
      dfloat xn = x[n];
      dfloat yn = y[n];
      
      insFlowField2D(t, xn, yn, U+n, V+n, P+n);
    }
  }


}
