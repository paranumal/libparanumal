

kernel void ellipticFooTri2D(const int Nelements,
			      const dfloat * restrict Pq,
			      dfloat * restrict Rq){
  
  
  for(int e=0;e<Nelements;++e;outer0){
    for(int n=0;n<p_Np;++n;inner0){
      int idP = n + e*p_NpP;	
      int id  = n + e*p_Np;
      Rq[id] = Pq[idP];
    }
  }
}
