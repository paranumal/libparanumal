kernel void scaledAdd(const int N,
                      const dfloat alpha,
                      const dfloat * restrict x,
                      const dfloat beta,
                      dfloat * restrict y){
  
  for(int n=0;n<N;++n;tile(256)){
    if(n<N){
      y[n] = alpha*x[n] + beta*y[n];
    }
  }
}


kernel void scaledAddwOffset(const int N,
                      const dfloat alpha,
                      const int sourceOffset,
                      const dfloat * restrict x,
                      const dfloat beta,
                      const int destOffset,
                      dfloat * restrict y){
  
  for(int n=0;n<N;++n;tile(256)){
    if(n<N){
      y[n+destOffset] = alpha*x[n+sourceOffset] + beta*y[n+destOffset];
    }
  }
}