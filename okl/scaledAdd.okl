kernel void scaledAdd(const iint N,
                      const dfloat alpha,
                      const dfloat * restrict x,
                      const dfloat beta,
                      dfloat * restrict y){
  
  for(iint n=0;n<N;++n;tile(256)){
    if(n<N){
      y[n] = alpha*x[n] + beta*y[n];
    }
  }
}


kernel void scaledAddwOffset(const iint N,
                      const dfloat alpha,
                      const iint sourceOffset,
                      const dfloat * restrict x,
                      const dfloat beta,
                      const iint destOffset,
                      dfloat * restrict y){
  
  for(iint n=0;n<N;++n;tile(256)){
    if(n<N){
      y[n+destOffset] = alpha*x[n+sourceOffset] + beta*y[n+destOffset];
    }
  }
}