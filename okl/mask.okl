
kernel void mask(const dlong Nmasked,
                 const dlong    * restrict maskIds,
                       dfloat * restrict q){

  for(dlong n=0;n<Nmasked;++n;tile(256)){
    if(n<Nmasked){
      q[maskIds[n]] = 0.;
    }
  }
}
