
kernel void dotMultiply(const int N,
			const dfloat * restrict w,
			const dfloat * restrict v,
			dfloat * restrict wv){

  for(iint n=0;n<N;++n;tile(256)){
    if(n<N){
      wv [n] = w[n]*v[n];
    }
  }

}
